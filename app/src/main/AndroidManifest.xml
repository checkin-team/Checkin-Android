<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.checkin.app.checkin">

    <uses-permission
        android:name="android.permission.AUTHENTICATE_ACCOUNTS"
        android:maxSdkVersion="22" />
    <uses-permission
        android:name="android.permission.GET_ACCOUNTS"
        android:maxSdkVersion="22" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission
        android:name="android.permission.USE_CREDENTIALS"
        android:maxSdkVersion="22" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:replace="android:theme">
        <activity
            android:name=".Home.SplashActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Auth.AuthActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
        <activity
            android:name=".Home.HomeActivity"
            android:label="@string/title_activity_home"
            android:screenOrientation="sensorPortrait"
            android:theme="@style/AppTheme"
            tools:replace="screenOrientation" />
        <activity android:name=".User.NonPersonalProfile.UserProfileActivity" />
        <activity
            android:name=".Menu.SessionUserActivity"
            android:label="@string/title_activity_session_user"
            android:screenOrientation="portrait"
            android:theme="@style/SessionTheme"
            android:windowSoftInputMode="adjustPan" />
        <activity
            android:name=".Social.ChatActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".Profile.ShopProfile.ShopProfileActivity2"
            android:screenOrientation="portrait" />
        <activity android:name=".Misc.TransactionActivity" />
        <activity android:name=".Home.StoryActivity" />
        <activity android:name=".Misc.FilterTransactions" />
        <activity android:name=".Misc.FaqActivity" />
        <activity
            android:name=".Session.ActiveSessionActivity"
            android:windowSoftInputMode="adjustNothing" />
        <activity android:name=".Shop.ShopPublicProfile.ShopActivity" />
        <activity android:name=".Shop.ShopPrivateProfile.ShopActivity" />
        <activity
            android:name=".Notifications.NotificationActivity"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="com.checkin.app.checkin.notifications" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Shop.BusinessFeaturesActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat.Light" />
        <activity
            android:name=".Shop.SignUpVerifyActivity"
            android:parentActivityName=".Home.HomeActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat.Light" />
        <activity android:name=".Utility.SelectCropImageActivity" />

        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />

        <activity
            android:name="com.facebook.FacebookActivity"
            android:configChanges="keyboard|keyboardHidden|screenLayout|screenSize|orientation"
            android:label="@string/app_name" />
        <activity
            android:name="com.facebook.CustomTabActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="@string/fb_login_protocol_scheme" />
            </intent-filter>
        </activity>
        <activity android:name=".Shop.ShopInfoActivity" />
        <activity android:name=".User.PersonalProfile.UserProfilePersonalActivity" />
        <activity android:name=".User.PersonalProfile.EditPersonalProfile" />
        <activity
            android:name=".Maps.MapsActivity"
            android:label="@string/title_activity_maps"
            android:theme="@style/Theme.AppCompat.Light" />
        <activity android:name=".Shop.ShopInfoActivity" />

        <service
            android:name=".Auth.AuthenticatorService"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.accounts.AccountAuthenticator" />
            </intent-filter>

            <meta-data
                android:name="android.accounts.AccountAuthenticator"
                android:resource="@xml/authenticator" />
        </service>
        <service android:name=".Data.MessagingService">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <service android:name=".Maps.FetchAddressIntentService" />
        <service android:name=".Auth.DeviceTokenService" />

        <activity android:name=".Shop.ShopReviewsActivity"></activity>
    </application>

</manifest>